# VOPK — الواجهة الموحدة لإدارة الحزم

**VOPK** هي الجيل القادم من **واجهة موحدة لإدارة الحزم** تعمل عبر عدة توزيعات، تم تطويرها وصيانتها بواسطة **فريق GP**.
توفر **واجهة أوامر واحدة (CLI)** لجميع توزيعات لينكس الرئيسية — بنفس الأوامر، وسلوك متوقع، وتجربة استخدام أنظف وأسهل.

---

> 🆕 **الإصدار 1.2.0 – دعم Homebrew وBSD**
>
> * دعم أصلي لـ **Homebrew** على macOS (بدون الحاجة إلى sudo)
> * دعم جديد لأنظمة **BSD**:
>
>   * FreeBSD: `pkg`
>   * NetBSD: `pkgin / pkg_add`
>   * OpenBSD: `pkg_add`
> * تحسينات على الواجهة للأنظمة غير المبنية على لينكس (تحسين معلومات النظام والشبكة)
>
> 🆕 **الإصدار 1.0.0 – أكبر تحديث في تاريخ APKG**
>
> * إعادة تسمية المشروع من **APKG → VOPK**
> * أول إصدار **مستقر رسميًا** (الخروج من مرحلة البيتا)
> * واجهة وتجربة استخدام جديدة، مع اختصارات ذكية ومخرجات أوضح للمبتدئين
> * تحسين سلوك وضع المحاكاة (Dry‑Run) بدون أسئلة غير ضرورية
> * دعم اختياري لواجهة **vmpkg**
> * العديد من إصلاحات الأخطاء وتحسينات الأمان

سواء كنت تستخدم Debian أو Ubuntu أو Arch أو Fedora أو openSUSE أو Alpine أو Void أو Gentoo أو macOS (Homebrew) أو أنظمة BSD — **VOPK يعمل مباشرة بدون تعقيد**.

---

## 🌐 الموقع الرسمي

[https://gpteamofficial.github.io/vopk/](https://gpteamofficial.github.io/vopk/)

---

## 🚀 لماذا VOPK؟

مدراء الحزم التقليديون مرتبطون بتوزيعاتهم:

* `apt` / `apt-get` لتوزيعات Debian وUbuntu
* `pacman` لـ Arch
* `dnf` / `yum` لـ Fedora / RHEL / CentOS
* `zypper` لـ openSUSE
* `apk` لـ Alpine
* `xbps` لـ Void
* `emerge` لـ Gentoo

**VOPK يحل هذه المشكلة** عبر تقديم **واجهة أوامر موحدة ومتناسقة** فوق جميع هذه المدراء.

أمثلة:

```bash
# بدلًا من:
sudo dnf update
sudo pacman -S neovim
sudo apt remove firefox

# يمكنك استخدام:
sudo vopk update
sudo vopk install neovim
sudo vopk remove firefox
```

نفس الصيغة. نفس طريقة التفكير. على جميع التوزيعات المدعومة.

---

## ✨ أبرز الميزات في الإصدار 1.0.0

* ✅ **اسم وهوية جديدة**: `apkg` → **`vopk`**
* ✅ **أول إصدار مستقر رسميًا** (1.0.0)
* ✅ **واجهة موحدة** عبر جميع توزيعات لينكس الرئيسية
* ✅ دعم: **apt / apt-get / pacman / dnf / yum / zypper / apk / xbps / emerge**
* ✅ **اكتشاف تلقائي** لمدير الحزم المناسب
* ✅ **وضع احتياطي باستخدام dpkg** (يعمل حتى بدون apt)
* ✅ دعم اختياري لـ **vmpkg** عند عدم توفر مدير حزم للنظام
* ✅ تجربة استخدام صديقة للمبتدئين (رسائل أوضح وإعدادات آمنة)
* ✅ **وضع محاكاة نظيف (Dry‑Run)**:

  * يعرض ما سيحدث فقط
  * بدون أي أسئلة تأكيد
* ✅ اختصارات قصيرة وسهلة الاستخدام
* ✅ تمرير مباشر للأوامر إلى مدير الحزم الأصلي (للسكربتات)
* ✅ تنفيذ Bash آمن (`set -euo pipefail`)
* ✅ ملف Bash واحد بدون أي اعتماديات خارجية (عدا مدير الحزم)
* ✅ أدوات مساعدة مفيدة: معلومات النظام، تثبيت أدوات التطوير، إصلاح DNS، وغيرها

تم تصميم VOPK ليكون سهلًا للمبتدئين وقويًا للمستخدمين المتقدمين والسكربتات.

---

## 📦 مدراء الحزم المدعومون

| الواجهة الخلفية     | التوزيعات / الملاحظات             |
| ------------------- | --------------------------------- |
| **apt / apt-get**   | Ubuntu، Debian، Mint، PopOS، Kali |
| **pacman**          | Arch، Manjaro، EndeavourOS        |
| **dnf**             | Fedora                            |
| **yum**             | CentOS، RHEL (قديم)               |
| **zypper**          | openSUSE                          |
| **apk**             | Alpine Linux                      |
| **xbps-install**    | Void Linux                        |
| **emerge**          | Gentoo                            |
| **brew**            | macOS (Homebrew)                  |
| **pkg**             | FreeBSD                           |
| **pkgin / pkg_add** | NetBSD / OpenBSD                  |
| **dpkg (احتياطي)**  | أنظمة Debian بدون apt/apt-get     |
| **vmpkg (اختياري)** | عند عدم توفر مدير حزم للنظام      |

يقوم VOPK باكتشاف الواجهة المناسبة تلقائيًا.

---

## 🔥 بديل مباشر لـ APT (وغيره)

يمكن لـ VOPK العمل **مع أو بدون apt**:

* عند توفر `apt` أو `apt-get` → يتم استخدامه تلقائيًا
* عند عدم توفره ووجود `dpkg` → يتم التحويل إلى وضع **dpkg فقط**
* في وضع dpkg يمكن تثبيت ملفات `.deb` المحلية وتنفيذ العمليات الأساسية

مثال:

```bash
# الاستخدام العادي (مع apt)
sudo vopk install htop

# على أنظمة Debian المصغرة بدون apt:
sudo vopk install ./custom-package.deb
```

هذا يجعل VOPK قادرًا على العمل حتى على الأنظمة المصغرة والحاويات.

---

## 🤝 تكامل vmpkg (اختياري)

إذا كان **vmpkg** مثبتًا:

* سيتم استخدامه تلقائيًا عند عدم توفر مدير حزم للنظام
* يمكن استدعاؤه مباشرة عبر VOPK

```bash
vopk vm install my-package
vopk vmpkg list
```

يتم تمرير متغيرات البيئة تلقائيًا عند استخدام vmpkg.

> ملاحظة: VOPK لا يستبدل منطق vmpkg، بل يوفر واجهة موحدة وأبسط للتعامل معه.

---

## 🏗 التثبيت

> ℹ️ المسارات تفترض أن المشروع مستضاف تحت `gpteamofficial/vopk`.

### التثبيت بسطر واحد (مستحسن – يجب التشغيل بصلاحيات root)

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/gpteamofficial/vopk/main/src/installscript.sh)
```

### طريقة بديلة (بدون تشغيل مباشر كـ root)

```bash
curl -fsSL https://raw.githubusercontent.com/gpteamofficial/vopk/main/src/installscript.sh | sudo bash -s -- -y
```

---

## 🛠 الصيانة (تحديث / إصلاح / إعادة تثبيت / حذف)

```bash
curl -fsSL https://raw.githubusercontent.com/gpteamofficial/vopk/main/src/updatescript.sh | sudo bash
```

من هذه القائمة يمكنك:

* تحديث VOPK
* إصلاح التثبيت
* إعادة تثبيت VOPK
* حذف VOPK
* حذف VOPK مع النسخ الاحتياطية

---

## 📚 الاستخدام الأساسي

```bash
vopk update
vopk upgrade
vopk full-upgrade
vopk install <الحزمة>
vopk remove  <الحزمة>
vopk purge   <الحزمة>
vopk autoremove
vopk search <نمط>
vopk list
vopk show <الحزمة>
vopk clean
```

### الاختصارات

```bash
vopk i   <pkg>   # تثبيت
vopk rm  <pkg>   # إزالة
vopk up          # تحديث + ترقية
vopk fu          # ترقية كاملة
vopk ls          # عرض القائمة
vopk s   <pattern> # بحث
vopk si  <pkg>   # معلومات الحزمة
```

---

## 📂 إدارة المستودعات

```bash
vopk repos-list
vopk add-repo <args...>
vopk remove-repo <pattern>
```

السلوك يختلف حسب مدير الحزم، وهو محافظ افتراضيًا لضمان الأمان.

---

## 🧠 أدوات النظام والمطورين

```bash
vopk sys-info
vopk kernel
vopk disk
vopk mem
vopk top
vopk ps
vopk ip
vopk install-dev-kit
vopk fix-dns
vopk doctor
```

أمر `vopk doctor` مفيد جدًا لتشخيص المشاكل على الأنظمة المصغرة والحاويات وWSL.

---

## 🌍 الخيارات العامة ومتغيرات البيئة

الخيارات العامة:

* `-y, --yes` الموافقة التلقائية
* `-n, --dry-run` عرض ما سيحدث بدون تنفيذ وبدون أسئلة
* `--no-color` تعطيل الألوان
* `--debug` عرض معلومات التصحيح
* `-q, --quiet` إخفاء الرسائل المعلوماتية

متغيرات البيئة الخاصة بـ VOPK:

```bash
VOPK_SUDO=""
VOPK_SUDO="doas"
VOPK_ASSUME_YES=1
VOPK_DRY_RUN=1
VOPK_NO_COLOR=1
VOPK_DEBUG=1
VOPK_QUIET=1
```

كما يتم دعم متغيرات APKG القديمة للتوافق الخلفي.

---

## 🧱 أهداف التصميم

* واجهة موحدة عبر جميع التوزيعات
* إعدادات آمنة بشكل افتراضي
* أداء سريع وبأقل تعقيد
* شفافية كاملة وعدم إخفاء أخطاء المدراء الأصلية
* تجربة سهلة ومناسبة للمبتدئين

---

## 💚 الشكر والتقدير

تم تطوير وصيانة VOPK بواسطة **فريق GP**.
كان المشروع يُعرف سابقًا باسم **APKG**، وهذا الإصدار هو أول إصدار مستقر رسميًا تحت اسم **VOPK**.

مشروع يهدف إلى تبسيط إدارة الحزم عبر جميع البيئات.

---

## ⭐ دعم المشروع

إذا أعجبك VOPK:

* ⭐ أضف نجمة للمستودع
* 🐛 افتح بلاغات أو اقترح ميزات
* 📣 شارك المشروع مع مجتمعك وأصدقائك
* 🔁 قم بحزمته لتوزيعتك المفضلة أو صور الأنظمة

إدارة حزم سعيدة 🐧
